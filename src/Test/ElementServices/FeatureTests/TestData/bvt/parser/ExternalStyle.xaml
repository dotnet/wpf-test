

<DockPanel Name="body"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:media="clr-namespace:System.Windows.Media;assembly=PresentationCore" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:MappingPIGen1="clr-namespace:System.Windows.Media;assembly=PresentationCore">
    <Canvas>
        <Button Name="border1" Style="{DynamicResource CustomBackground}" />	
        <DockPanel>
            <Button Name="dock1" Style="{DynamicResource BottomToolBar}"> 
                <Button Name="b1" Style="{DynamicResource ManageUsersButton}" Content="Manage User Accounts" />
            </Button>
            <DockPanel DockPanel.LastChildFill="true">
                <Button Name="Logo" Style="{DynamicResource LOGO}" />
                <DockPanel DockPanel.LastChildFill="true">
                    <Button Name="button1" Style="{DynamicResource VerticalDockStacker}">
                        <TextBlock Name="t1" Style="{DynamicResource UserTileText}">Patrick</TextBlock>
                    </Button>
                </DockPanel>
            </DockPanel>
        </DockPanel>
    </Canvas>
</DockPanel>
