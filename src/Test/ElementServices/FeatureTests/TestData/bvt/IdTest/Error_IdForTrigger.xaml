<cmn:CustomCanvas Name="id"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:cmn="clr-namespace:Microsoft.Test.Serialization.CustomElements;assembly=TestRuntime" xmlns:MappingPIGen1="clr-namespace:Microsoft.Test.Serialization.CustomElements;assembly=TestRuntime"
>
      <Canvas Name="canvas">
        <Canvas.Resources>
            <Style x:Key="style" TargetType="{x:Type Button}">
                <Setter Property="Template">
                
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border>
                        <Canvas Name="id" />
                    </Border>
                <ControlTemplate.Triggers>

                <Trigger Name="idforTrigger" Property="Background" Value="Red">
                <Setter TargetName="id" Property="Foreground" Value="Red" />
                </Trigger>
                </ControlTemplate.Triggers>
                </ControlTemplate>
                
                </Setter>
            </Style>
        </Canvas.Resources>        
      </Canvas>
</cmn:CustomCanvas>
