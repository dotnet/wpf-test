<Window
    xmlns:x = "http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns   = "http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    x:Class = "Microsoft.Test.Xaml.Localization.GenericLocBamlTest">

    <DockPanel>

        <Button>
            <Button.ContextMenu>
                <ContextMenu x:Uid="ContextMenu_1">LLLContent1</ContextMenu>
            </Button.ContextMenu>
        </Button>

        <TextBox>
            <TextBox.ContextMenu>
                <ContextMenu x:Uid="ContextMenu_2"><TextBlock x:Uid="TextBlock_1">LLLContent2</TextBlock></ContextMenu>
            </TextBox.ContextMenu>
        </TextBox>

        <Menu>
          <MenuItem>
            <MenuItem>
                <MenuItem.ContextMenu>
                    <ContextMenu x:Uid="ContextMenu_3">
                        <MenuItem x:Uid="MenuItem_1">LLLContent3</MenuItem>
                    </ContextMenu>
                </MenuItem.ContextMenu>
            </MenuItem>

            <MenuItem>
                <MenuItem.ContextMenu>
                    <ContextMenu x:Uid="ContextMenu_4">
                        <TextBox x:Uid="TextBox_1" Text="LLLText1" />
                    </ContextMenu>
                </MenuItem.ContextMenu>
            </MenuItem>
          </MenuItem>
        </Menu>

    </DockPanel>

</Window>
