<Project DefaultTargets="CustomControl;UseControl" xmlns="http://schemas.microsoft.com/developer/msbuild/2003"> 

    <Target Name="CustomControl"> 
        <MSBuild Projects="CustomControl.proj" /> 
    </Target> 


    <Target Name="UseControl"> 
        <MSBuild Projects="UseControl.proj" /> 
    </Target> 

<!--  Need for MSBuild2 target for cleanup -->
<Import Project="TestCommon.target" />

</Project>
