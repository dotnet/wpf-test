<Page
    xmlns       = "http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x     = "http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Page.Resources>
        <ResourceDictionary>
            <x:SynchronousMode>4</x:SynchronousMode>
        </ResourceDictionary>
    </Page.Resources>

    <x:String>
        Emerald
        <x:SynchronousMode>423</x:SynchronousMode>
        Ruby
    </x:String> 

    <x:SynchronousMode><x:Null /></x:SynchronousMode>

    <x:SynchronousMode></x:SynchronousMode>

    <x:SynchronousMode><TextBox /></x:SynchronousMode>

    <x:SynchronousMode><x:Double>0</x:Double></x:SynchronousMode>

    <x:SynchronousMode><x:String>zzz</x:String></x:SynchronousMode>

    <x:SynchronousMode><x:Type></x:Type></x:SynchronousMode>

    <x:SynchronousMode>abc</x:SynchronousMode>

    <Polygon x:SynchronousMode="{x:Null}" />  

    <UnknownObject><x:SynchronousMode>5454</x:SynchronousMode></UnknownObject>

    <Rectangle x:SynchronousMode="3">
        <x:SynchronousMode>4</x:SynchronousMode>
    </Rectangle>

</Page>