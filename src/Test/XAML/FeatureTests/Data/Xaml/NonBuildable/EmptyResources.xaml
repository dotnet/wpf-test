<!--  Resource objects with no content -->
<Page
    xmlns       = "http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:sys   = "clr-namespace:System;assembly=mscorlib"
    xmlns:x     = "http://schemas.microsoft.com/winfx/2006/xaml">

    <Page.Resources>

        <Button x:Key="Key1" />

        <sys:Double x:Key="Key2" />

        <SolidColorBrush x:Key="Key4" />
        
        <Storyboard x:Key="Key5"></Storyboard>

        <Style TargetType="{x:Type Button}">
            <Setter />
        </Style>

        <Style TargetType="{x:Type ContentControl}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Grid />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>  
        </Style>

        <Style TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate></ControlTemplate>
                </Setter.Value>
            </Setter>  
        </Style>

        <Style><Style.TargetType></Style.TargetType></Style>

        <Style><Style.BasedOn><Style /></Style.BasedOn></Style>

        <Style><Style.BasedOn></Style.BasedOn></Style>

        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>

        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries></ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>

        <Button />

        <Style />
        
        <Storyboard />

        <Button><Button /></Button>

    </Page.Resources>

</Page>