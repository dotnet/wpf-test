<!-- Regression test, checking the value of a binding after load -->
<StackPanel xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation">
    <Button Name="b1" Content="Content" />
    <Label Content="{Binding ElementName=b1,Path=Content,FallbackValue=Fallback}" />
</StackPanel>