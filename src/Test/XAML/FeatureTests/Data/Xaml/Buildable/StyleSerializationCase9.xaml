



<cmn:CustomCanvas xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:cc="http://XamlTestTypes" xmlns:cmn="clr-namespace:Microsoft.Test.Serialization.CustomElements;assembly=TestRuntime" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:S="sys"  xmlns:MappingPIGen2="clr-namespace:Microsoft.Test.Serialization.CustomElements;assembly=TestRuntime">
        <cmn:CustomCanvas.Resources>
        <!-- 9 in style2 matrix-->
            <Style x:Key="Style1" TargetType="{x:Type Button}">
                <Setter Property="Foreground" Value="White" />
                <Setter Property="Template">
                
                <ControlTemplate TargetType="{x:Type Button}">
                    <TextBlock x:Name="myText" Text="Text content" />
                <ControlTemplate.Triggers>

                <Trigger Property="Button.IsPressed" Value="False">
                <Setter TargetName="myText" Property="Foreground" Value="Red" />
                </Trigger>
                </ControlTemplate.Triggers>
                </ControlTemplate>
                
                </Setter>
            </Style>            
        </cmn:CustomCanvas.Resources>
    <Button Name="Button2" />
</cmn:CustomCanvas>

