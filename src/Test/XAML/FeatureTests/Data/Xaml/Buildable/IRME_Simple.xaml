<!--Box implments IRME and AMEET(DimensionalExpression).  In this scenario we expect ReceiveMarkupExtension() to be called-->

<CustomRoot xmlns="http://XamlTestTypes"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:co="http://XamlTestTypes">
    <Box Name="Box1" Footprint="{Dimension 10, 20}" Volume="{Dimension 10, 20, 30}"/>
</CustomRoot>