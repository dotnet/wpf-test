        ; Using a total of 512 instructions, 508 of them are ADD instructions
        ps_3_0
        def c0, 0.003937, 0.003937, 0.003937, 0.0    ; 2 / 508 = 0.003937
        def c1, 0.0, 0.0, 0.0, 1.0
        dcl_texcoord v0.xy
        dcl_2d s0
        texld r0, v0, s0
        mov r1, c1
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        add r1, r1, c0
        mul r2, r1, r0 ; at this point r1 should be almost 2.0, so we are roughly doubling the RGB values of the input texture.
        mov oC0, r2
