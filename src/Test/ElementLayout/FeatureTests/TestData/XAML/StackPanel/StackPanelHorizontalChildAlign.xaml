<!-- StackPanel Case.  Horizontal Orientation with Vertical Alignments on Child -->
<Grid xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation">
    
    <Grid.Resources>
        <Style xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" TargetType="{x:Type Border}">
            <Setter Property="Background" Value="LimeGreen" />
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="BorderBrush" Value="DarkGreen" />
        </Style>
        <Style xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" TargetType="{x:Type TextBlock}">
            <Setter Property="Text" Value="Text" />
            <Setter Property="Margin" Value="10" />
            <Setter Property="FontSize" Value="40" />
          <Setter Property="FontFamily" Value="Tahoma" />
        </Style>
    </Grid.Resources>
    
    <StackPanel Orientation="Horizontal" Height="300" Width="450" Background="CornflowerBlue">
        <Border VerticalAlignment="Stretch">
            <TextBlock />
        </Border>
        <Border VerticalAlignment="Top">
            <TextBlock />
        </Border>
        <Border VerticalAlignment="Center">
            <TextBlock />
        </Border>
        <Border VerticalAlignment="Bottom">
            <TextBlock />
        </Border>
    </StackPanel>
    
</Grid>
