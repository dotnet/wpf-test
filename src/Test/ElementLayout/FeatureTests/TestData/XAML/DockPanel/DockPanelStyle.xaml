<DockPanel Background="DarkBlue" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation">

        <DockPanel.Resources>
                <Style x:Key="style" TargetType="{x:Type DockPanel}">
                    <Setter Property="HorizontalAlignment" Value="Left" />
                    <Setter Property="VerticalAlignment" Value="Top" />
                    <Setter Property="Height" Value="200" />
                    <Setter Property="Width" Value="200" />
                    <Setter Property="Background" Value="Crimson" />
                    <Setter Property="Margin" Value="5" />
                </Style>
        </DockPanel.Resources>

        
        <DockPanel Style="{DynamicResource style}" />
                
</DockPanel>
