<!-- Regression Bug : NewLayout - with a ScrollViewer in DockPanel HorizontalScrollBarVisibility="Hidden" does not work-->

<DockPanel Margin="5" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" Background="LightGreen" Width="300">

  <ScrollViewer
   VerticalScrollBarVisibility="Auto"
   HorizontalScrollBarVisibility="Hidden"
   Height="200"
   HorizontalAlignment="Stretch">

    <ListBox Background="lightgray" Height="Auto" HorizontalAlignment="Stretch" BorderThickness="0" Padding="0">

      <ListBoxItem Margin="0" Padding="0">
        <Border Background="pink" Width="1000" Height="50" />
      </ListBoxItem>

    </ListBox>

  </ScrollViewer>


</DockPanel>
